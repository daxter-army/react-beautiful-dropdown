'use strict';

const reactDropdown = require('../lib/ReactBeautifulDropdown');
const assert = require('assert').strict;

assert.strictEqual(reactDropdown(), 'Hello from reactDropdown');
console.info('reactDropdown tests passed');
